import React from 'react';
import { View, Text } from 'react-native';

import { APP_NAME } from '@constants';

const App = () => {
  return (
    <View>
      <Text>{APP_NAME}</Text>
    </View>
  );
};

export default App;
